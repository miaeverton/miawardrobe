{
  "name": "mia-outfits",
  "version": "2.0.0",
  "description": "Mia's personal AI stylist — visual outfit boards from real wardrobe",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step — static site with Netlify Functions'"
  }
}
